import type { NextConfig } from "next";

const nextConfig: NextConfig = {
	/* config options here */
	images: {
		domains: ["ssl.pstatic.net", "images.unsplash.com"], // Replace with your actual image hostnames
	},
};

export default nextConfig;
